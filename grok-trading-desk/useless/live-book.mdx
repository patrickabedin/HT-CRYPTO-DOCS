---
title: "USELESS live paper book"
description: "Continuous SHADOW paper for USELESS_SCALP_BRAIN. Desk calendar reads settle, not Telegram."
---

## What it is

`scripts/useless_live_book.py`. Continuous SHADOW paper for the $50@20x 5m brain.

**Auto paper is killed** (`PAPER_BOOK_KILLED`). Replay 5–19 Sep: 138 ENTERs, WR 24.6%, −\$323. The worker was ALIVE (~32k runs) after the 9 Sep "fix" and still booked nothing on the calendar. Same 5m brain feeds CLIP GO; a 2% take with those stops is worse. Keep `scripts/useless_scalp.py` as Patrick's advisory clock. Calendar keeps the four 5 Sep human fills.

Mute Telegram without killing the desk: PnL at `/?g=SCALP&e=USELESS_SCALP_BRAIN` reads settle books.

## Clock and size

`grok-useless-live-book.timer` 30s. Same $50 @ 20x.

## Radio

None required. `USELESS_TELEGRAM=0` is the host default.

## Host

`USELESS_SETTLE_PATH` under `/var/lib/skynet-market/`.

## State / settle

Detect / settle / scout timers keep running while radio is muted.

## Gates

Engine health cadence 30s. STALE/DEAD via [engine health](/grok-trading-desk/ops/engine-health).

## How Patrick uses it

Calendar is the book. Quiet Telegram is not a dead engine.

## Decisions that still bind

Do not mix this settle file into Grade B paper totals.
