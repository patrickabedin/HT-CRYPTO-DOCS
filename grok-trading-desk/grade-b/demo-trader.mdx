---
title: "Bitget demo trader"
description: "Opt-in Bitget Demo paper. Grade B TRIGGERED only. $50 margin at 20x. Never a live key."
---

## What it is

`scripts/demo_trader.py` + `scripts/bitget_demo.py` copy **Grade B TRIGGERED** freezes onto Bitget Demo Trading. Same size as the calendar: **$50 @ 20x**.

Scout B (~500/day) and ARMED-but-not-triggered stay paper. Grade A stays off demo. Hunter TAPE / UNIQUE / brain TAKEs do not ingest.

## Clock and size

- Timer: `grok-demo-trader.timer` every 15s
- Notional: $1000
- Demo force-close: 24h max hold (mark), or profit giveback once peak uPnL ≥ $12 and mark gives back ≥ 35% of that peak
- Host timer allow 480s so flatten + attach can finish

Paper USDT is not created by the API. Until demo `usdtEquity ≥ 50` the trader prints `NO_EQUITY`.

Desk Last/uPnL for demo rows (and attached paper) use Bitget `mark` / `unrealizedPL`, not public WS last. File can lag REST by one 15s poll.

## Radio

No extra Telegram from ingest. Cards already published by chief / armed watcher. Open-book HIGH ≥ \$30 is `grade_b_upl_alert` (source `grade_b_upl_high`), not this timer inventing a second bot path.

## Host

- Flag `BITGET_DEMO_TRADING=1`
- Kinds `BITGET_DEMO_KINDS=GRADE_B`
- Leverage `BITGET_DEMO_LEVERAGE=20`
- Env file on the host (mode 0600, not git). Never put values here.
- `paptrading: 1` is hardcoded in `bitget_demo.py`

Armed watcher must load the same demo env. Without it, TRIGGERED ingest is `flag_off`.

## State / settle

- `/var/lib/skynet-market/demo_trader.json`
- `/var/lib/skynet-market/demo_settle.json`

## Gates

- `kind=GRADE_B`
- Engine is a trading family (not `NO_SUPPORTED_SETUP`, not Hunter `BREAKOUT`)
- Champion score 45–74
- Freeze returned entry, stop, and at least one TP
- Ten-slot union with paper
- One Bitget position per symbol/side — same-side stacks add another $50 @ 20x
- Junk / naked fills (no SL/TP) and UNIQUE-shaped keys with an empty episode are flattened
- A first fill without card SL+TP is refused: re-read, attach card levels, flatten **that** side if still unprotected. Empty `holdSide` is not covered (`bf651f1`)
- Flatten cooldown blocks ingest (`flatten_cooldown`, silent). Do not leave paper-only smokes behind — see [13 Sep notes](/grok-trading-desk/grade-b/2026-09-13-upl-honesty)

## How Patrick uses it

Honesty check for the calendar, not live money. Do not swap demo keys for live. A live trade-enabled key on this path would fire every Grade B TRIGGERED.

Do not flatten leftover XRP SHORT that he left as an exchange-demo leftover.

## Decisions that still bind

Leftover Hunter TAPE / BREAKOUT demo legs are flattened. Bitget demo is Grade B TRIGGERED only. Live `AUTO_ORDER_EXECUTION` stays off.
