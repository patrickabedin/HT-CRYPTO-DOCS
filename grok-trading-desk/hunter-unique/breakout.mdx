---
title: "BREAKOUT"
description: "5m close through the prior session range. Packs only when grain and sweep did not. Not Grade B demo."
---

<Warning>
  Unique no longer TAKEs BREAKOUT. [Tape BREAKOUT is also killed](/grok-trading-desk/hunter-tape/breakout). `detect_breakout` stays for diagnostics only. Do not retune GP / SR / XM from this page.
</Warning>

## What it is

`detect_breakout` in `scripts/hunter_ripe_watch.py`. Needs at least 5 bars before the candidate break candle. Retest band around the anchored pre-break level: 0.15%.

This is **not** Grade B `BREAKOUT_CONFIRMATION` and **not** a Bitget demo family. Leftover Hunter BREAKOUT demo legs flatten.

On Unique, `pack_own_setup` drops BREAKOUT (`UNIQUE_SKIP_ENGINES`). If GP/SR failed and the tape is `EXTREME_*`, Unique may still try EXTREME_MOMENTUM.

## Why Unique killed it (19 Sep)

Live Unique book: **n=455**, **WR 37.6%**, **−\$1,247** — the entire Unique USD hole. 11 / 13 Sep fired **82 / 89 / 200** Unique BREAKOUT tickets. Same packer as Tape when GP/SR miss. No Unique-only edge. Restoring muted persist without this kill would re-spray settle.

That is resource stress with no edge. Historical Unique BREAKOUT rows stay on the calendar. New Unique TAKEs will not be BREAKOUT.

## Clock and size

5m. Same SL floor 0.4% and R:R 1.5. Chase: refused when last already ran ≥ 1R past the trigger (trigger→SL).

## Radio

TAPE cards when hunter radio is on. Hunter telegram TAPE does **not** call demo ingest. Unique radio stays muted.

## Host

State: `/var/lib/skynet-market/hunter_breakout.state.json` (`skynet_hunter_breakout_state_v0`). Retained across ticks and restamped; never invent actionable. That file is Tape retain, not Unique TAKEs.

## State / settle

Retained BREAKOUT / EXTREME rows stay for the live Tape episode. A symbol flipping GP→BREAKOUT is a new Tape pair and may POST.

## Gates

- GP and SR both failed to pack (Tape ripe)
- Close through prior high (long) / low (short), or wait-retest at the anchored level
- Unique: packer result is discarded

## How Patrick uses it

Wait the retest on Tape. Unique brain will not TAKE this name as BREAKOUT. Do not enable `BITGET_DEMO_ENGINES=BREAKOUT`.

## Decisions that still bind

R:R minimum was lowered from 2.0 to 1.5 because 2.0 emptied `ripe[]`. That is a Tape packer change, not a profitability claim. Unique kill is independent of that floor.
